<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Users</title>
</head>
<body>
    <main>
        <h1>Users registered</h1>
        <% if(users.length > 0) { %>
            <% users.forEach((user) => { %>
                <h2><%= user.name %></h2>
                <p>dummy content</p>
            <% }) %> 
        <% } else { %>
            <h1>Not users registered</h1>
        <% } %>
    </main>
</body>
</html>